commands:
    01_run_migrations:
        command: "php artisan migrate --force"
        leader_only: true
    02_cache_config:
        command: "php artisan cache:clear && php artisan config:cache && php artisan route:cache && php artisan view:cache"
        leader_only: true
